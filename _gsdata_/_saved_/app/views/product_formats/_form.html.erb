<%= form_for(@product_format) do |f| %>
  <% if @product_format.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_format.errors.count, "error") %> prohibited this product_format from being saved:</h2>

      <ul>
      <% @product_format.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :style %><br />
    <%= f.select :style, ["FOLDED", "GATEFOLD", "FLAT"] %>
    </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>

   <div class="field">
    <%= f.label :width %><br />
    <%= f.text_field :width %>
  </div> 
    <div class="field">
    <%= f.label :height %><br />
    <%= f.text_field :height %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
