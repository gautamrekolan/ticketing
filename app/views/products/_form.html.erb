<%= form_for([@theme,@product]) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_type_id %><br />
    <%= f.collection_select :product_type_id, ProductType.all, :id, :description %>
  </div>
  <div class="field">
    <%= f.label :product_format_id %><br />
    <%= f.collection_select :product_format_id, ProductFormat.all, :id, :description %>
  </div>
    <div class="field">
    <%= f.label :paper_id %><br />
    <%= f.collection_select :paper_id, Paper.all, :id, :description %>
  </div>
  <div class="field">
    <%= f.label :theme_id %><br />
    <%= f.collection_select :theme_id, Theme.all, :id, :name %>
  </div>
    <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
