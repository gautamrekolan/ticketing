<%= form_for(@monogram) do |f| %>
  <% if @monogram.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monogram.errors.count, "error") %> prohibited this monogram from being saved:</h2>

      <ul>
      <% @monogram.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
